# Этап 0 — отчёт по репозиторию

**Дата:** 21 сентября 2026
**Что делали:** разбор репозитория и заведение памяти проекта. Только чтение.

Ветка `main` создана и запушена — теперь есть стабильная точка, которая
не удаляется автоматически.

## Заведена память проекта (закоммичено в `main`)

- `CLAUDE.md` — что за проект, устройство, все правила
  («Как работаем» + «Жёсткие правила»).
- `STATUS.md` — что выяснили и план аудита.
- Старую записку `СТАТУС.md` оставил как историю.

## Для чего репозиторий

Рабочие материалы контент-завода SCALIA: свод правил, справочники по
песням и площадкам, готовые тексты, контролёр `check.py`. Приложения тут
нет — завод работает как скилл внутри Hermes на сервере.

## Ветки (живое состояние на GitHub)

| Ветка | Что | Статус |
|---|---|---|
| `main` | завод SCALIA + память | **создана сейчас, стабильная** |
| `claude/telegram-bot-setup-gvlv0k` | завод SCALIA (`cefcc4f`) | была «по умолчанию»; **целиком вошла в `main`** |
| `claude/bot-power-charging-be0aii` | **другой проект — бот про питание** | не слита, свежая, **не трогать** |
| `claude/hermes-shvksj` | прошлая линия | уже удалена сервером |

## Секреты

В репозитории чисто: `.env` в `.gitignore`, в истории его нет,
`.env.example` — только примеры без реальных значений.

## Что можно удалить (предложение, не делаю)

Ветка `claude/telegram-bot-setup-gvlv0k` полностью содержится в `main` —
её можно убрать, **но сначала** надо переключить «ветку по умолчанию» на
GitHub на `main` (сейчас по умолчанию стоит она). Ветку
`claude/bot-power-charging-be0aii` удалять нельзя — там несросшаяся
работа по боту питания.

## Расхождение с инструкцией

Ты просила работать в `main`, но её на сервере не было — я создал её из
текущего состояния завода. Прошлые `claude/*`-ветки удаляются
автоматически, `main` обычно живёт. Если нужна другая база — скажи.

## ⚠️ Отдельно: утечка на скриншотах

На скриншотах прошлой сессии на экран попал **пароль приложения Yandex**
для ящика `valleya@yandex.ru`. Раз он был на экране — считать раскрытым.
Рекомендую перевыпустить пароль приложения в Yandex
(Почта → Пароли приложений: удалить старый, создать новый).

## Дальше

Готов начинать проверку сервера с **`/root/s.py`** (Этап 1).
Жду слово **«дальше»**.
