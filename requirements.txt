aiogram==3.15.0
anthropic==0.68.0
python-dotenv==1.0.1

# БД: профиль, приёмы пищи, тренировки, прогресс.
sqlalchemy[asyncio]>=2.0.35,<2.1
asyncpg>=0.29.0,<0.30

# Распознавание голосовых сообщений (Whisper-совместимый API).
httpx>=0.27,<1.0

# Планировщик напоминаний (приёмы пищи/вода/тренировка) — следующий этап.
apscheduler>=3.10.4,<3.11

# Графики прогресса (вес/объёмы/калорийность) — следующий этап.
matplotlib>=3.9.2,<4.0

# Тесты.
pytest>=8.3.3,<9.0
